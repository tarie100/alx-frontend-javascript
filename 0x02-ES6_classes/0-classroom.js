/**
 * Represents a class ClassRoom.
 */
export default class ClassRoom {
  /**
   * Creates a new ClassRoom instance.
   * @constructor
   * @param {Number} maxStudentsSize - The max num of
   * students in a class.
   */
  constructor(maxStudentsSize) {
    this._maxStudentsSize = maxStudentsSize;
  }
}